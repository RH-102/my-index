name: Update Index and Deploy

on:
  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - "scripts/**"
      - "*.html"
      - "requirements.txt"
      - ".github/workflows/update-index.yml"

  schedule:
    - cron: "10 16 * * 1-5"
      timezone: "America/New_York"

concurrency:
  group: index-update-and-pages
  cancel-in-progress: false

permissions:
  contents: write
  pages: write
  id-token: write

jobs:

  update-index:

    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:

      - name: Checkout repository
        uses: actions/checkout@v6


      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"


      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt


      - name: Update prices and weights
        run: |
          python scripts/update_prices.py


      - name: Update quarterly Top 125
        continue-on-error: true
        run: |
          python scripts/update_top100.py


      - name: Update market risk dashboard
        continue-on-error: true
        run: |
          set +e
          python scripts/update_risk.py > data/risk_update.log 2>&1
          status=$?
          cat data/risk_update.log
          echo "exit_code=$status" >> data/risk_update.log
          exit 0


      - name: Save updated data
        run: |

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add \
            data/latest.csv \
            data/index_history.csv \
            data/holdings_history.csv \
            data/top100_history.csv \
            data/risk_dashboard.csv \
            data/risk_history.csv \
            data/risk_update.log

          if git diff --cached --quiet; then

            echo "No data changes to commit"

          else

            git commit -m "Update daily index data"

            git pull --rebase origin main

            git push origin main

          fi


      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5


      - name: Upload website
        uses: actions/upload-pages-artifact@v4
        with:
          path: "."


      - name: Deploy website
        id: deployment
        uses: actions/deploy-pages@v4
